# Core Execution v14

V14 freezes the first variable-width pure-function string ABI over the unchanged
v13 canonical vocabulary. ABI v1 remains frozen; string-bearing signatures use
ABI v2 with ordered descriptors, exact UTF-8 payloads, and bounded runtime text
operations.
