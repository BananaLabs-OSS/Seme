# Core Execution v17

This additive module freezes compositional immutable records using the existing
`RecordType`, `RecordField`, `RecordConstruct`, and `FieldRead` semantics. Its
checked artifacts are generated by `reference/go/cmd/execution-module-v17` and
accepted by `scripts/check-execution-v17.sh`.
