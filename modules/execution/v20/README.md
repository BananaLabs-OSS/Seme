# Core Execution v20

This additive module freezes ordered `EffectInvoke` statements that reference
Foundation `Effect` identities. Capability identity and realization remain in
Foundation and target contracts rather than being duplicated in Core. Checked
artifacts are generated by `reference/go/cmd/execution-module-v20` and accepted
by `scripts/check-execution-v20.sh`.
