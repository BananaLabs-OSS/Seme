# Core Execution Semantics v31

Version 31 adds neutral optional values: one value type and explicit `None`
and `Some` constructors. It does not equate absence with any ecosystem's null,
nil, undefined, zero, exception, pointer, or omitted-field behavior.
