# Core Execution v12

V12 freezes the generic structured pure-function validation and runtime profile
without adding schemas to v11. The checked module artifact records the new
semantic revision; target-specific ABI evidence lives under `targets/wasm`.
