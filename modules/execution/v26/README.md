# Core Execution v26

This additive module introduces receiver-bound methods and explicit state
transition values. A transition contains an updated state and a returned result;
it does not imply hidden mutation, source object identity, or target storage.
