# Core Execution v39

Version 39 adds typed `NativeInvocation` boundaries. Canonical code may retain a
precisely identified native callable while targets without the matching runtime
reject realization honestly.
