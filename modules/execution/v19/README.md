# Core Execution v19

This additive module freezes the provider-neutral `When` statement for
one-sided structured choice. It composes with the mutable-place vocabulary
from v18. Its checked artifacts are generated by
`reference/go/cmd/execution-module-v19` and accepted by
`scripts/check-execution-v19.sh`.
