# Core Execution v13

V13 adds provider-neutral total-return control flow and bounded text
expressions: explicit `If` branches, short-circuit `BooleanOr`, UTF-8
`StringEqual`, and `StringConcat`. Both branches of the initial executable
`If` profile are required `Block` references.
