# Core Execution v15

This additive module introduces immutable lexical local bindings, ordered
binding statements, and local reads. Its checked G1 and SemOne artifacts are
generated by `reference/go/cmd/execution-module-v15` and accepted by
`scripts/check-execution-v15.sh`.
