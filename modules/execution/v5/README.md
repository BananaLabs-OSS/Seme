# Core Execution Semantics v5

Revision 5 preserves v4 and adds the minimum general control semantics required
to choose between Result variants: string literals, an empty-string test, and a
typed conditional expression.
