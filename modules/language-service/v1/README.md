# Live Language Service Contract v1

This directory freezes the provider-neutral incremental language-service
schema. `module.g1` is the readable construction projection and `module.seme`
is the canonical compiled artifact.

See [`spec/live-language-service-v1.md`](../../../spec/live-language-service-v1.md)
for ordering, digest, lift-result, diagnostic, source-mapping, and last-valid
semantics.

Run `./scripts/check-language-service-v1.sh` from the repository root.
