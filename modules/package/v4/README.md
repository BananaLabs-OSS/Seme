# Package Contract v4

Package v4 (`...b000@...b004`, parent `...b003`) preserves every Package v3
schema and export byte-for-byte while changing its execution authority to Core
Execution v36 (`...9000@...9024`). This revision is required for package graphs
whose owned semantic closure contains `BooleanNot`; it adds no language-specific
package concept.
