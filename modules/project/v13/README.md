# Project Contract v13

Project v13 (`...e000@...e032`, parent `...e031`) adds
`PlacedProjectSnapshot` (`e02c`). It binds one exact Project-v12
`EffectProjectSnapshot` to one neutral Target Contract v1 `ExecutionPlan` and
a content revision.

The revision imports exact Target Contract v1 authority
`...c000@...c001`. Project-v12's complete package, dependency, configuration,
resource, durable-state, presentation, ordered-transport, controlled-effects,
and replay authority remains unchanged. Target mechanics, provider selection,
and downstream products remain outside this Project contract.
