# Project Contract v5

Project v5 composes the authenticated Project v4 dependency graph with a
Package v3 complete package graph. The new snapshot binds semantic declaration
ownership to the same project without copying or redefining Execution meaning.

It pins Execution v35, Package v3, and Dependency v1 and preserves all earlier
Project contract revisions byte-for-byte.
