# Project Contract v6

Project v6 adds `ConfiguredProjectSnapshot` (`e021`) to bind an authenticated
Project v5 snapshot to one Configuration v1 graph and a content revision. It
imports Configuration `4000@4001`, Package `b000@b003`, Execution
`9000@9023`, and Dependency `f000@f001`; earlier Project revisions remain
byte-for-byte unchanged.
