# Project Contract v11

Project v11 (`...e000@...e030`, parent `...e00e`) adds
`TransportProjectSnapshot` (`e026`). It binds one exact Project-v10
`DurableProjectSnapshot` to one neutral Ordered Transport v1 plan and a content
revision.

The revision adds the exact Ordered Transport v1 import `...10000@...10001`.
Project-v10's complete package, dependency, configuration, resource, durable
state, and source-presentation authority remains unchanged. Transport framing
and provider realization stay outside this Project contract.
