# Project Contract v10

Project v10 (`...e000@...e00e`, parent `...e00b`) adds
`DurableProjectSnapshot` (`e025`). It binds one exact Project-v9
`ResourceProjectSnapshot` to one Durable State v1 plan, one Source Presentation
v1 manifest, and a content revision.

The revision adds exact Durable State v1 and Source Presentation v1 imports;
all Project-v9 pins and declarations remain unchanged.
