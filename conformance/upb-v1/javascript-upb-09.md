# JavaScript UPB-09 evidence

Status: claimed 2026-09-11 by `scripts/check-javascript-upb-09.sh`.

The cumulative six-module native JavaScript project declares explicit clock,
seeded-random, command/state/result records and pure next, dispatch, and replay
functions. Its project graph now derives neutral effect ownership from native
`console.log` source and the canonical `observability.log` identity; Project
base and Package Detail consume the same provider graph without duplicating
semantic effect representation.

Controlled Effects v1 binds the injected nondecreasing clock, frozen signed-i64
LCG, one capability-authorized Boolean effect request, replay/rejection policy,
and exact bounds. Project v12 binds that plan to the authenticated Project-v11
chain. Native JavaScript, canonical Seme, standalone Wasm, and pinned Pulp agree
on values and the exact ordered logical effect trace, including signed overflow.
The host/runtime placement tests keep live sampling and external delivery out
of the semantic program and reject ambient substitutions.

Identical source and selections reproduce Project v12. Invalid owners,
callables, replay identity, algorithms, bounds, unknown provider fields, stale
prior artifacts, and output collisions reject without publication. The full
UPB01–UPB08 chain, ordinary ES-module projection, and semantic re-lift remain
green.
