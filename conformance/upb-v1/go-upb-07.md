# Go UPB-07 evidence

Status: claimed 2026-09-10 by `scripts/check-go-upb-07.sh`.

The cumulative ordinary Go project retains the complete UPB-06 application
and adds one pure durable-state planner. One state family has versions 1 and 2,
one directed migration, validators for both versions, a positive current
revision, checked overflow, a bounded canonical payload, and a logical key.

Durable State v1 describes the neutral family and ordered storage operations.
Project v10 binds its exact plan and Source Presentation v1 to the complete
Project-v9 snapshot. Source Presentation records native aliases and their
authenticated import/source origins without adding alias types to Execution or
Core.

The gate proves all seven mapped evidence classes:

1. The cumulative ordinary project passes offline, including deterministic
   2,080-case native planner corpora and race tests.
2. Repeated lifts reproduce the complete contract and 14-artifact project
   bundle with two detached resource blobs.
3. Native and canonical planner observations agree for all 2,080 cases.
4. A source-free loader and report reproduce the complete prior authority,
   durable family, migration, port requirements, resources, and presentation.
5. Standalone Wasm and pinned Pulp match the pure planner. Separately, the
   authenticated Durable artifact derives a sealed Go-host profile that drives
   exact Load/compare-exchange identities, order, bounds, and opaque-token
   threading. Exact pinned-Pulp resolution rejects filesystem and SQLite as
   substitutes for the missing CAS provider; no Pulp DurablePort behavior is
   claimed.
6. Authenticated projection emits ordinary Go, preserves aliases and opaque
   bytes, passes native tests, re-lifts to equal normalized semantic authority,
   changes source-bound revisions honestly, and reaches an exact second-cycle
   bundle/report fixed point.
7. Contract, family, presentation, source, binding, payload, capability,
   token, port-realization, artifact, bundle-shape, mixing, symlink, and
   existing-output adversaries reject without partial authority or state
   commits.

This bounded claim does not imply arbitrary databases, transactions, retries,
queries, broad Go source aliases, a Pulp CAS realization, or general project
compatibility.
