# JavaScript UPB-05 evidence

Status: claimed 2026-09-11 by `scripts/check-javascript-upb-05.sh`.

The cumulative gate proves all seven evidence classes for three native ES
modules with typed configuration and ordered lifecycle. JavaScript JSDoc
records, native classes, functions, imports, `Result<Record,string>` values,
defaults, a typed validator, and three dependent initializers lift into one
Execution-v36 graph. Provider-independent commands bind the same run into
Project base v8, source inventory v8, Package detail/v4, Dependency v1,
Configuration v3, and Project v8 authorities.

The authenticated configuration executor validates defaults and order before
executing configuration, policy, and application initialization. Native
JavaScript and canonical execution produce the same ready runtime. Original
and projected ES modules re-lift to byte-identical G1 and retain native
behavior. The scalar project entry agrees across native JavaScript, canonical
evaluation, the standalone canonical Wasm cell, and pinned Pulp.

Equivalent file enumeration reproduces. Ambient host reads, invalid lifecycle
edges, stale Package authority, malformed boundary requests, and publication
collisions reject atomically. This is a bounded lifecycle claim, not support
for arbitrary JavaScript module initialization or Node APIs.
