# Lua UPB-10 acceptance design

Status: claimed 2026-09-12 by `scripts/check-lua-upb-10.sh`.

Lua UPB-10 extends the cumulative native project through neutral Target v1 and
Project v13. Planning consumes authenticated Project-v12 canonical authority,
never Lua filenames or syntax. Provider evidence is namespaced `lua-upb10`.

The bounded project derives 33 requirements. Twenty-nine have exact Wasm/Pulp
realizations. DurablePort, TransportPort, live clock sampling, and external
effect delivery remain four typed Lua-host native islands. Exact-only policy
therefore reports four impossibilities and publishes no deployment; mixed
policy retains explicit boundaries without silently adapting or dropping them.

Acceptance requires the complete UPB09 gate, reproducible contracts, validated
deployment packages, two byte-identical source-free placements, strict report
reopening, exact-only non-publication, tamper and collision rejection, and
closed canonical envelopes. Only `scripts/check-lua-upb-10.sh` may claim this
cell.
