# Lua UPB-01 evidence

Status: claimed 2026-09-12 by `scripts/check-lua-upb-01.sh`.

The sole cumulative gate passed all seven UPB evidence classes for the existing
two-file cumulative Lua application. Source discovery classified two semantic
Lua files plus generated, ignored, vendored, and opaque files deterministically
and bound their exact digests through Project v2.

The Lua provider lifted both native files directly into canonical Execution v35.
A provider-owned manifest derivation bound the exact `Apply` interface and
`observability.log` effect into neutral Package-v1 and Project-v1 authority.
Independent project/source artifacts reproduced byte-for-byte.

Canonical projection produced a fresh native Lua project. Neovim executed the
original and projected applications over the same 2,048-command corpus with
structurally identical values, state, errors, and effect traces. The projected
source re-lifted to the exact original canonical G1 bytes. The inherited UAB-11
gate independently retained canonical evaluator, standalone Wasm, and pinned
Pulp parity plus capability-denial and malformed-boundary rejection.

Opaque, generated, ignored, vendored, and manifest bytes were preserved
exactly. Source digest drift, symlink traversal, malformed/forged semantic
graphs, denied capabilities, repeated output paths, and publication collisions
rejected without partial artifacts.

The run used the existing Neovim Lua runtime, Node.js, and cached Go 1.26.0
toolchain through process-local environment variables. No software was
installed and no persistent environment setting changed.

This bounded source/project claim does not imply broad Lua, LuaRocks, native
island, or Workbench support.
