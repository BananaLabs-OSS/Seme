# JavaScript UPB-11 evidence

Status: claimed 2026-09-11 by `scripts/check-javascript-upb-11.sh`.

The sole cumulative gate passed all seven mapped evidence classes. A complete
ordinary ES-module project was lifted at revision 1, edited through the stable
canonical identity of `InitializePolicy`, projected as revision 2 with
`BuildPolicy`, validated natively, and re-imported with identity continuity.
The declaration, named import, and structured configuration reference changed;
matching historical comment prose remained byte-preserved.

Both revisions independently reproduced the complete Configuration, Resource,
Durable State, Source Presentation, Ordered Transport, Controlled Effects, and
Project-v8-through-v12 authority chain. Each then produced an authenticated
35-requirement Project-v13 placement with 31 exact realizations and four
explicit JavaScript-host native islands. Project v14 binds the exact before and
after authorities, Patch-v1 rename, monotonically increasing client revision,
and native-validation transcript.

The gate proved deterministic twin reconciliation and final publications,
strict source-free reopening, native behavioral parity, canonical fixed points,
and atomic rejection for forged identities, stale native snapshots, simultaneous
native and semantic changes, ambiguous declarations, scope/name conflicts,
invalid identifiers, metadata tampering, and output collisions. The complete
UPB-10 predecessor also retained native, canonical, standalone-Wasm, and pinned-
Pulp runtime evidence.

The run used the existing cached Go 1.26.0 toolchain through process-local
`GOROOT`, `PATH`, and `GOTOOLCHAIN=local`. No dependency or toolchain was
installed and no persistent environment setting changed.

This is a bounded project-reconciliation claim. It does not imply arbitrary
JavaScript support, general refactoring, native compiler realization, automatic
native-island execution, or a completed Workbench consumer.
