# Lua UPB-07 evidence

Status: claimed 2026-09-12 by `scripts/check-lua-upb-07.sh`.

The cumulative gate adds native Lua v1 and v2 state records, validators, and a
v1-to-v2 migration to the exact Project-v9 graph. A project-neutral selection
resolves them by authenticated ownership and semantic identities. Durable State
v1 binds the family, current version, migration, canonical codec, bounds,
signed-i64 errors, string keys, and ordered Load/CompareAndSwap capabilities;
Project v10 binds this authority and neutral source presentation.

Native Lua, canonical Seme, standalone Wasm, and pinned Pulp agree on migration
over zero, positive, and negative values. The independent neutral DurablePort
suite proves authorization, bounded canonical payloads, opaque tokens, exact
Load/CAS ordering, conflicts, race-safe atomic memory writes, and repeated
stress execution. No filesystem, database, or Pulp storage provider is claimed.

Identical inputs reproduce all Project-v10 artifacts. Owner, validator,
migration, codec, stale authority, unknown-field, and output-collision
adversaries reject without partial publication. Projection preserves and
re-lifts the complete cumulative project exactly.
