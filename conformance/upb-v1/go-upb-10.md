# Go UPB-10 evidence

Status: claimed 2026-09-11 by `scripts/check-go-upb-10.sh`.

Go UPB-10 extends the authenticated Project-v12 project with deterministic,
project-derived target requirements and one neutral Target-v1 execution plan.
Project v13 binds that independently valid plan to the exact predecessor.
Provider and deployment authority remain outside Core semantics.

The cumulative gate proves all seven mapped evidence classes:

1. The complete ordinary Go project retains every UPB-09 native race test and
   its deterministic 4,096-observation controlled-effects corpus.
2. Independent source-free placements derive the same 92-requirement closure
   and publish byte-identical closed bundles, including Project-v13.
3. The exact pure execution partition inherits native/canonical state, result,
   random, clock, and logical-effect parity from the complete UPB-09 gate.
4. All 92 requirements resolve explicitly: 88 exact and four native islands.
   Every Target-v1 fidelity value is preserved independently; provider rules,
   evidence, dependencies, policy, and typed boundaries are authenticated.
5. Standalone Wasm and pinned Pulp retain the 4,096-case parity proof. The
   deployment carries the exact canonical VM and Pulp cell manifest; clock
   sampling, effect delivery, DurablePort, and TransportPort remain four named
   Go-host islands rather than receiving false Wasm support.
6. Source-free ordinary-Go projection builds offline and re-lifts to exact
   canonical construction and execution meaning. Independent reopening of the
   same Project-v12 authority regenerates the same plan and report while the
   project layer continues to retain honest source-provenance revisions.
7. Exact-only policy yields 88 exact and four impossible resolutions with four
   diagnostics, zero boundaries, and no publication. Malformed requirements,
   rules, evidence, dependencies, fidelity, boundaries, catalogs, launch
   bindings, mixed/tampered files, and existing destinations reject without a
   partial observable deployment.

The deterministic placement bundle contains an authenticated provider catalog,
Target-v1 plan, Project-v13 graph, canonical VM Wasm, pinned Pulp cell manifest,
native-island launch manifest, source-free report, and completion manifest.

This bounded claim does not imply arbitrary Go project import, general Go
runtime or standard-library compatibility, automatic native-island execution,
ambient WASI capabilities, or broad target discovery. It proves an honest,
closed, reproducible physical placement for the cumulative fixture.
