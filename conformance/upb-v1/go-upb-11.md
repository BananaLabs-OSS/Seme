# Go UPB-11 evidence

Status: claimed 2026-09-11 by `scripts/check-go-upb-11.sh`.

Go UPB-11 extends the authenticated Project-v13 authority with deterministic,
transactional native-source reconciliation. A canonical identity-bound rename
of `example.test/go-uab-11/transport.EqualI64` to `SameI64` updates 23 typed
occurrences across two packages without changing the declaration's Seme
identity. Project v14 binds the exact prior and resulting Project-v13
authorities, canonical Patch-v1 transaction, revisions, and native-validation
digest. Workbench and editor behavior remain external to Seme Core.

The cumulative gate proves all seven mapped evidence classes:

1. The complete ordinary Go project passes native tests before and after the
   edit and retains the inherited deterministic 4,096-observation corpus.
2. Both complete 92-requirement revisions lift, place, and publish independently;
   each resolves 88 exact requirements and four declared native islands.
3. The edited project retains inherited native/canonical behavioral parity while
   preserving stable canonical identity across the native spelling change.
4. Dependencies, target fidelity, and all four runtime boundaries remain
   authenticated rather than being silently widened by reconciliation.
5. Standalone Wasm and pinned Pulp retain the inherited exact-partition parity;
   Go-host clock, effect, durable-state, and transport islands remain explicit.
6. Projection is atomic, builds and tests offline, preserves the original source,
   re-lifts to the independently expected canonical graph, and produces two
   byte-identical reconciliation and Project-v14 publications.
7. Stale identity, malformed bindings, overlaps, collisions, symlinks,
   native-test failures, tampering, and existing output destinations reject
   without exposing a partial result.

The published authority contains the complete projected native project, prior
and resulting provider manifests and G1 graphs, deterministic validation
transcript, canonical Patch-v1 instance, compact Project-v14 graph, and
completion hashes. Independent readers authenticate and reconstruct the
semantic transition rather than trusting producer reports.

This bounded claim does not imply arbitrary Go editing, general refactoring,
three-way merging, broad standard-library/runtime compatibility, or a completed
Workbench consumer. It proves the project-neutral reconciliation seam needed by
those later milestones.
