# JavaScript UPB-07 evidence

Status: claimed 2026-09-11 by `scripts/check-javascript-upb-07.sh`.

The cumulative gate extends the exact JavaScript Project-v9 graph with native
JSDoc-defined v1 and v2 state records, pure validators, and one v1-to-v2
migration. The project-neutral durable selection resolves those declarations
by authenticated package ownership and semantic identities. Durable State v1
binds the family, current version, migration, canonical codec, bounds, signed
i64 errors, string keys, and ordered Load/CompareAndSwap capabilities. Project
v10 binds that authority and an intentionally empty, valid source-presentation
manifest to the complete Project-v9 snapshot.

Native JavaScript, canonical Seme, standalone Wasm, and pinned Pulp agree on
the migration for zero, positive, and negative i64 values. Malformed record
boundaries reject. The independent neutral host DurablePort suite proves
authorization, bounded canonical payloads, opaque tokens, exact Load/CAS
ordering, conflicts, and atomic in-memory writes; no filesystem, database, or
Pulp storage provider is claimed.

Identical inputs reproduce every Project-v10 artifact. Missing owners or
callables, a signature-invalid migration substitution, a noncanonical codec,
unknown manifest fields, stale Project-v9/Resource-v1 authorities, and output
collisions reject without publication. The complete UPB-06 projection,
resources, dependencies, configuration lifecycle, and cross-target behavior
remain green with the state module included.
