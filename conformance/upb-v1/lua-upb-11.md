# Lua UPB-11 evidence

Status: claimed 2026-09-12 by `scripts/check-lua-upb-11.sh`.

The cumulative gate lifted native revision 1, addressed `InitializePolicy` by
its stable semantic identity, projected revision 2 with `BuildPolicy`, ran the
complete native configuration lifecycle, and re-imported the same canonical
identity. Declaration, returned export, and typed configuration reference
changed while historical prose was byte-preserved.

Both revisions independently reproduced Configuration, Resource, Durable State,
Source Presentation, Ordered Transport, Controlled Effects, and Project-v8
through Project-v12 authority. Each produced a 33-requirement Project-v13
placement with 29 exact realizations and four explicit Lua-host native islands.
Project v14 binds the exact before/after projects, Patch-v1 rename, client
revision, and native-validation transcript.

Twin final publications were byte-identical. Forged identities, invalid names,
stale native snapshots, simultaneous native/semantic changes, replacement
collisions, metadata tampering, and output collisions rejected atomically. The
cached Go 1.26 toolchain was used process-locally; nothing was installed and no
persistent environment setting changed.
