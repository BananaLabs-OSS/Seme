# Go UPB-08 evidence

Status: claimed 2026-09-10 by `scripts/check-go-upb-08.sh`.

The cumulative ordinary Go project retains UPB-07 and adds a sealed ordered
request/response stream service. Ordered Transport v1 describes neutral
commands, events, sequencing, correlation, retained replay, whole-frame bounds,
and a capability-authenticated port. Project v11 binds the exact transport
selection to the complete Project-v10 snapshot.

The gate proves all seven mapped evidence classes:

1. The cumulative ordinary project passes race tests and a deterministic
   4,096-observation native transport corpus.
2. Repeated lifts reproduce every contract and the complete 16-artifact bundle.
3. Native and canonical observations agree for all 4,096 cases.
4. Source-free loading and reporting reproduce the cumulative authority,
   selected transport, package graph, resources, state, and presentation.
5. Standalone Wasm and pinned Pulp agree with the pure transport planner. The
   separately authenticated Go host port proves exact bytes, strict framing,
   fragmentation/coalescing, capability preauthorization, post-commit send
   failure, and exact retry. Pinned Pulp rejects TransportPort; its synchronous
   opaque call carrier is not represented as an authoritative stream port.
6. Authenticated projection emits native ordinary Go, passes its tests,
   re-lifts to exact canonical meaning, and reaches a second-cycle bundle and
   report fixed point.
7. Contract, binding, frame, payload, sequence, correlation, replay, retained
   size, capability, placement, artifact, mixing, symlink, and output-collision
   adversaries reject without partial authority or state commits.

This bounded claim does not imply arbitrary protocols, sockets, asynchronous
Pulp streams, unbounded messages, realtime replication, browser transports, or
general Go project compatibility.
