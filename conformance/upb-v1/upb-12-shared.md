# Shared UPB-12 evidence

Go, JavaScript, and Lua claim UPB-12 together through
`scripts/check-upb12-shared.sh`; there is no language-specific substitute.

The gate constructs one source-free, six-package `seme.upb12/service`
authority twice and proves byte-identical publication. It independently
projects ordinary Go, JavaScript ES-module, and Lua projects. Each project
passes its native tests after Seme evidence is excluded from application
execution, and each source-bound re-lift returns the exact authoritative G1.

One generated 4,096-command corpus, including signed-i64 wraparound, produces
the same values and ordered authorized effect through all three native
projections, canonical evaluation, standalone Wasm, and Pulp pinned at
`acc66ca61fe69c5f2c4093bc55e13aeac6dcc001`. The whole project retains exact
dependency, configuration, resource, durable-state, transport, controlled
effect, replay, target-placement, and revision authorities.

The same identity-bound `InitializePolicy` to `BuildPolicy` edit is applied
to every native projection. All three edited projects pass native validation
and re-lift to one byte-identical revision-2 graph. Patch-v1 and Project-v14
bind that exact transition to a combined three-language validation transcript,
and repeated finalization reproduces byte-for-byte.

Dependency, resource, revision, capability, boundary-value, malformed-graph,
mixed-bundle, symlink, forged-native-test, and destination-collision
adversaries reject without partial publication. This completes the frozen
UPB-v1 matrix at 36/36; it does not claim arbitrary language, ecosystem,
runtime, standard-library, FFI, editor, or game-engine compatibility.
