# Quota application proof fixture

This is an ordinary Go package. It deliberately contains no generated source,
Seme annotations, adapters, or runtime dependency. Its exported `Admit` policy
is the first package/application boundary lifted and executed independently.
