# Wasm/Pulp target-plan fixture

This ordinary Go package uses the standard `log` dependency and performs an
observable logging effect. It contains no Seme, Wasm, or Pulp annotations.
Its ordinary `AdmitRequest` and `AdmitResponse` structs are the scoped source
for canonical record lifting and generated application codecs.
