import fs from "node:fs";const [mode,file]=process.argv.slice(2),vectors=JSON.parse(fs.readFileSync(file,"utf8"));if(mode==="requests")for(const item of vectors.valid)console.log(JSON.stringify({arguments:item.arguments}));else if(mode==="expected")for(const item of vectors.valid)console.log(JSON.stringify({value:item.result,effects:[]}));else throw new Error("lua_upb05_vectors.mode");
