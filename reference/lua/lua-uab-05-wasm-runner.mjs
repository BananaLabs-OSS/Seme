import fs from "node:fs";
const [wasmPath,vectorsPath,mode]=process.argv.slice(2);if(!wasmPath||!vectorsPath)throw new Error("usage: lua-uab-05-wasm-runner WASM VECTORS [--tsv]");
const wasm=fs.readFileSync(wasmPath),vectors=JSON.parse(fs.readFileSync(vectorsPath,"utf8"));
const encode=(item)=>{if(typeof item.use_double!=="boolean")throw new TypeError("lua.boolean_boundary");const request=Buffer.alloc(17);request[0]=item.use_double?1:0;request.writeBigInt64LE(BigInt(item.amount),1);request.writeBigInt64LE(BigInt(item.value),9);return request;};
async function invoke(request){const{instance}=await WebAssembly.instantiate(wasm,{}),api=instance.exports,input=api.pulp_alloc(request.length),output=api.pulp_alloc(4),length=api.pulp_alloc(4);new Uint8Array(api.memory.buffer).set(request,input);let status;try{status=api.pulp_on_call(0,0,input,request.length,output,length);}catch{return{status:-1};}if(status!==0)return{status};const view=new DataView(api.memory.buffer),pointer=view.getUint32(output,true);if(view.getUint32(length,true)!==8)throw new Error("lua.uab05.response_size");return{status,value:view.getBigInt64(pointer,true)};}
const valid={};for(const item of vectors.valid){const request=encode(item),observed=await invoke(request);if(observed.status!==0||observed.value.toString()!==item.result)throw new Error(`lua.uab05.wasm_mismatch:${item.name}`);valid[item.name]=observed.value.toString();if(mode==="--tsv"){const response=Buffer.alloc(8);response.writeBigInt64LE(BigInt(item.result));console.log(`valid\t${item.name}\t${request.toString("hex")}\t${response.toString("hex")}`);}}
const malformed=[["empty",Buffer.alloc(0)],["short",Buffer.alloc(16)],["long",Buffer.concat([encode(vectors.valid[0]),Buffer.from([0])])],["invalid-selector",encode(vectors.valid[0])]];malformed[3][1][0]=2;for(const[name,request]of malformed){if((await invoke(request)).status===0)throw new Error(`lua.uab05.malformed_accepted:${name}`);if(mode==="--tsv")console.log(`malformed\t${name}\t${request.toString("hex")}\t-`);}if(mode!=="--tsv")console.log(JSON.stringify({valid,malformed:malformed.map(([name])=>name)}));
