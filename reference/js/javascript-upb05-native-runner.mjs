import {pathToFileURL}from"node:url";import {Seme}from"./seme-values.mjs";
const root=process.argv[2];if(!root)throw new Error("usage: javascript-upb05-native-runner ROOT");globalThis.Seme=Seme;
const configuration=await import(`${pathToFileURL(`${root}/configuration.js`)}?native=${Date.now()}`);const policy=await import(`${pathToFileURL(`${root}/policy.js`)}?native=${Date.now()}`);const application=await import(`${pathToFileURL(`${root}/application.js`)}?native=${Date.now()}`);
const input={Enabled:configuration.DefaultEnabled(),Limit:configuration.DefaultLimit(),Namespace:configuration.DefaultNamespace()};const checked=configuration.ValidateLimit(input.Limit);if(checked.tag!=="ok")throw new Error("validator");const settings=configuration.InitializeConfig(input);if(settings.tag!=="ok")throw new Error("configuration");const selected=policy.InitializePolicy(settings.value);if(selected.tag!=="ok")throw new Error("policy");const runtime=application.Assemble(settings.value,selected.value);if(runtime.tag!=="ok"||!runtime.value.Ready||runtime.value.Total!==16n||runtime.value.Namespace!=="seme")throw new Error("runtime");
console.log(JSON.stringify({lifecycle:["configuration","policy","application"],ready:runtime.value.Ready,total:String(runtime.value.Total),namespace:runtime.value.Namespace}));
