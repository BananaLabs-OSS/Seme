# Configuration Contract v3

Configuration v3 retains the Configuration v2 immutable binding model and pins
Package v4, Core Execution v36, and Foundation v1. The revision authorizes v36
expressions and callables but adds no values, ambient environment access, or
source-language configuration behavior.
