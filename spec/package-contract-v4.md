# Package Contract v4

Package v4 is an authority-only additive revision of Package v3. All v3 schema
and field identities retain their meanings. The module imports exactly Core
Execution v36 (`9000@9024`), and package instances must be validated against
that authority. A v3 instance or Execution v35 contract is not substitutable.
